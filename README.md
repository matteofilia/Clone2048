# Clone2048

This is a clone of the popular game, 2048.

## Installation

This script / game requires pygame

If using a venv, run pip install pygame

If not, run python3 -m pip install pygame to install pygame to your local system python installation

## Running

Simply run python main.py

Alternatively, if python isn't python3, instead run python3 main.py
